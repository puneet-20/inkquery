warning: in the working copy of 'backend/routes/documents.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'backend/utils/geminiClient.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/backend/routes/documents.js b/backend/routes/documents.js[m
[1mindex 4f2952d..4cee439 100644[m
[1m--- a/backend/routes/documents.js[m
[1m+++ b/backend/routes/documents.js[m
[36m@@ -8,13 +8,8 @@[m [mimport { requireAuth } from '../utils/authMiddleware.js';[m
 [m
 const router = express.Router();[m
 [m
[31m-// Store the uploaded file in memory (not on disk) since we only need[m
[31m-// to read its text content, not keep the raw file around.[m
 const upload = multer({ storage: multer.memoryStorage() });[m
 [m
[31m-// Day 2: accept a PDF upload, extract its text.[m
[31m-// Day 3: chunk the text, embed each chunk, and store everything in Supabase.[m
[31m-// Day 6: require login, and tie the document to the logged-in user.[m
 router.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
   try {[m
     if (!req.file) {[m
[36m@@ -25,7 +20,6 @@[m [mrouter.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
       return res.status(400).json({ error: 'Only PDF files are supported right now.' });[m
     }[m
 [m
[31m-    // Step 1: extract text from the PDF[m
     const parsed = await pdfParse(req.file.buffer);[m
     const extractedText = parsed.text.trim();[m
 [m
[36m@@ -33,7 +27,6 @@[m [mrouter.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
       return res.status(400).json({ error: 'Could not find any readable text in this PDF.' });[m
     }[m
 [m
[31m-    // Step 2: create a document record, owned by the logged-in user[m
     const { data: document, error: docError } = await supabase[m
       .from('documents')[m
       .insert({ filename: req.file.originalname, user_id: req.user.id })[m
[36m@@ -42,11 +35,8 @@[m [mrouter.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
 [m
     if (docError) throw docError;[m
 [m
[31m-    // Step 3: split the text into chunks[m
     const textChunks = chunkText(extractedText);[m
 [m
[31m-    // Step 4: embed each chunk one at a time (sequential, to stay within free-tier rate limits)[m
[31m-    // and build the rows we'll insert into the chunks table.[m
     const chunkRows = [];[m
     for (const chunk of textChunks) {[m
       const embedding = await embedText(chunk, 'RETRIEVAL_DOCUMENT');[m
[36m@@ -57,7 +47,6 @@[m [mrouter.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
       });[m
     }[m
 [m
[31m-    // Step 5: store all chunks in one batch insert[m
     const { error: chunkError } = await supabase.from('chunks').insert(chunkRows);[m
     if (chunkError) throw chunkError;[m
 [m
[36m@@ -71,11 +60,17 @@[m [mrouter.post('/upload', requireAuth, upload.single('file'), async (req, res) => {[m
     });[m
   } catch (err) {[m
     console.error('Upload error:', err);[m
[32m+[m
[32m+[m[32m    if (err?.status === 503) {[m
[32m+[m[32m      return res.status(503).json({[m
[32m+[m[32m        error: 'The AI service is temporarily busy. Please wait a moment and try uploading again.',[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m
     res.status(500).json({ error: 'Something went wrong while processing the PDF.', details: err.message });[m
   }[m
 });[m
 [m
[31m-// Returns the logged-in user's own documents (for a document picker UI later).[m
 router.get('/', requireAuth, async (req, res) => {[m
   const { data, error } = await supabase[m
     .from('documents')[m
[36m@@ -87,9 +82,6 @@[m [mrouter.get('/', requireAuth, async (req, res) => {[m
   res.json({ documents: data });[m
 });[m
 [m
[31m-// Day 4-5: accept a question about a specific document, retrieve the most[m
[31m-// relevant chunks, and ask Gemini to answer using only that context.[m
[31m-// Day 6: require login, and make sure the document actually belongs to this user.[m
 router.post('/ask', requireAuth, async (req, res) => {[m
   try {[m
     const { documentId, question } = req.body;[m
[36m@@ -98,7 +90,6 @@[m [mrouter.post('/ask', requireAuth, async (req, res) => {[m
       return res.status(400).json({ error: 'Please provide both "documentId" and "question".' });[m
     }[m
 [m
[31m-    // Ownership check: make sure this document belongs to the logged-in user[m
     const { data: document, error: docError } = await supabase[m
       .from('documents')[m
       .select('id, user_id')[m
[36m@@ -113,12 +104,8 @@[m [mrouter.post('/ask', requireAuth, async (req, res) => {[m
       return res.status(403).json({ error: "You don't have access to this document." });[m
     }[m
 [m
[31m-    // Step 1: embed the question (using RETRIEVAL_QUERY, since this is a search query,[m
[31m-    // not a document being stored - Gemini tunes the embedding slightly differently for each)[m
     const questionEmbedding = await embedText(question, 'RETRIEVAL_QUERY');[m
 [m
[31m-    // Step 2: find the most similar chunks in Supabase using the match_chunks function[m
[31m-    // we defined in supabase_schema.sql (cosine similarity search via pgvector)[m
     const { data: matches, error: matchError } = await supabase.rpc('match_chunks', {[m
       query_embedding: questionEmbedding,[m
       match_document_id: documentId,[m
[36m@@ -134,7 +121,6 @@[m [mrouter.post('/ask', requireAuth, async (req, res) => {[m
       });[m
     }[m
 [m
[31m-    // Step 3: send the question + retrieved chunks to Gemini for a grounded answer[m
     const contextChunks = matches.map((m) => m.content);[m
     const answer = await generateAnswer(question, contextChunks);[m
 [m
[36m@@ -144,8 +130,15 @@[m [mrouter.post('/ask', requireAuth, async (req, res) => {[m
     });[m
   } catch (err) {[m
     console.error('Ask error:', err);[m
[31m-    res.status(500).json({ error: 'The AI service is temporarily busy. Please wait a moment and try again', details: err.message });[m
[32m+[m
[32m+[m[32m    if (err?.status === 503) {[m
[32m+[m[32m      return res.status(503).json({[m
[32m+[m[32m        error: 'The AI service is temporarily busy. Please wait a moment and try asking again.',[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    res.status(500).json({ error: 'Something went wrong while answering the question.', details: err.message });[m
   }[m
 });[m
 [m
[31m-export default router;[m
[32m+[m[32mexport default router;[m
\ No newline at end of file[m
[1mdiff --git a/backend/utils/geminiClient.js b/backend/utils/geminiClient.js[m
[1mindex 3cfeee1..1327426 100644[m
[1m--- a/backend/utils/geminiClient.js[m
[1m+++ b/backend/utils/geminiClient.js[m
[36m@@ -5,25 +5,42 @@[m [mdotenv.config();[m
 [m
 export const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });[m
 [m
[31m-// Model names as of mid-2026. Google occasionally renames/deprecates models,[m
[31m-// so if these ever 404 again, check https://ai.google.dev/gemini-api/docs/models[m
 export const EMBEDDING_MODEL = 'gemini-embedding-001';[m
 export const CHAT_MODEL = 'gemini-flash-latest';[m
 [m
[31m-// Turns a piece of text into a 768-number embedding (matches our Supabase schema).[m
[32m+[m[32masync function withRetry(fn, { retries = 2, delayMs = 1500 } = {}) {[m
[32m+[m[32m  for (let attempt = 0; attempt <= retries; attempt++) {[m
[32m+[m[32m    try {[m
[32m+[m[32m      return await fn();[m
[32m+[m[32m    } catch (err) {[m
[32m+[m[32m      const isOverloaded = err?.status === 503;[m
[32m+[m[32m      const isLastAttempt = attempt === retries;[m
[32m+[m
[32m+[m[32m      if (!isOverloaded || isLastAttempt) {[m
[32m+[m[32m        throw err;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      console.warn(`Gemini returned 503 (attempt ${attempt + 1}/${retries + 1}), retrying in ${delayMs}ms...`);[m
[32m+[m[32m      await new Promise((resolve) => setTimeout(resolve, delayMs));[m
[32m+[m[32m      delayMs *= 2;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 export async function embedText(text, taskType = 'RETRIEVAL_DOCUMENT') {[m
[31m-  const response = await ai.models.embedContent({[m
[31m-    model: EMBEDDING_MODEL,[m
[31m-    contents: [text],[m
[31m-    config: {[m
[31m-      taskType, // 'RETRIEVAL_DOCUMENT' when storing chunks, 'RETRIEVAL_QUERY' when embedding a question[m
[31m-      outputDimensionality: 768,[m
[31m-    },[m
[31m-  });[m
[32m+[m[32m  const response = await withRetry(() =>[m
[32m+[m[32m    ai.models.embedContent({[m
[32m+[m[32m      model: EMBEDDING_MODEL,[m
[32m+[m[32m      contents: [text],[m
[32m+[m[32m      config: {[m
[32m+[m[32m        taskType,[m
[32m+[m[32m        outputDimensionality: 768,[m
[32m+[m[32m      },[m
[32m+[m[32m    })[m
[32m+[m[32m  );[m
   return response.embeddings[0].values;[m
 }[m
 [m
[31m-// Sends the question + retrieved context chunks to Gemini and returns a plain-text answer.[m
 export async function generateAnswer(question, contextChunks) {[m
   const context = contextChunks.join('\n\n---\n\n');[m
 [m
[36m@@ -37,10 +54,12 @@[m [mQuestion: ${question}[m
 [m
 Answer:`;[m
 [m
[31m-  const response = await ai.models.generateContent({[m
[31m-    model: CHAT_MODEL,[m
[31m-    contents: prompt,[m
[31m-  });[m
[32m+[m[32m  const response = await withRetry(() =>[m
[32m+[m[32m    ai.models.generateContent({[m
[32m+[m[32m      model: CHAT_MODEL,[m
[32m+[m[32m      contents: prompt,[m
[32m+[m[32m    })[m
[32m+[m[32m  );[m
 [m
   return response.text;[m
[31m-}[m
[32m+[m[32m}[m
\ No newline at end of file[m
